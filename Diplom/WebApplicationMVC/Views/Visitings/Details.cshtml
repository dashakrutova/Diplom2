@model WebApplicationMVC.Models.Database.Visiting

@{
    // Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
    <link rel="stylesheet" href="~/css/action.css" asp-append-version="true" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>

<div>
        <h4>Посещение</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Status)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Status.Id)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.ScheduleEntry)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.ScheduleEntry.Id)
        </dd>
    </dl>
</div>
    <div class="button-container">
        <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-custom">Редактировать</a>
        <a asp-action="Index" class="btn btn-back">
            <i class="bi bi-arrow-left"></i>
            <span class="ms-1">Вернуться</span>
        </a>
    </div>
</body>
</html>
